package main

const helpText string = `
Usage: critty-theme [OPTION...]

    -s, --set value     search the colorscheme passed as value and set
                        it as the current colorscheme
    -h --help           shows this message
    -i, --interactive   shows the TUI mode of critty-theme
`
